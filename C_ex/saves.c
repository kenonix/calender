/*
날짜마다 정보를 지정하는것으로 정보를 저장한다.
+용량을 줄일 수 있다.
-반복 날짜는 어떻게 처리하나.
*/

#include "headers/main.h"

int save_object(const int type, const char * title, const int start_day, const int end_day){
  if(type == 0){ //일정 타입이 todo일경우

  }
  else if(type == 1){ //일정 타입이 스캐쥴인경우

  }
}
